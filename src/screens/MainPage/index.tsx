import React from "react";

import { HeroLane } from "../../components";

export const MainPage: React.FC = () => {
  return (
    <div style={{ padding: "20px 40px", maxHeight: "100vh" }}>
      <HeroLane />
    </div>
  );
};
